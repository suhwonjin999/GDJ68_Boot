<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.winter.app.member.memberDAO">
 
 <insert id="setJoin" parameterType="MemberVO">
 	INSERT INTO MEMBER
 	VALUES(#{username}, #{password}, #{name}, #{email}, #{birth}, now())
 </insert>
 
 <insert id="setMemberRole" parameterType="java.util.Map">
 	INSERT INTO MEMBER_ROLE
 	VALUES(#{roleNum}, #{username})
 </insert>
 
  <select id="getMember" parameterType="MemberVO" resultMap="getMemberResult">
  <!-- 데이터가 비어있지 않다면 중복이 아니다 -->	
  <!-- ROLE에 관련된 것은  MEMBERVO에 없음. -->
  	SELECT M.*, R.ROLENAME 
  	FROM MEMBER M
  		 <!-- 멤버는 한개이상의 계정을 가지고있다 -->
  		 INNER JOIN
  		 MEMBER_ROLE MR
  		 ON M.USERNAME = MR.USERNAME
  		 INNER JOIN
  		 ROLE R
  		 ON MR.ROLENUM = R.ROLENUM
  	<!-- 조건문 : M에 있는 USERNAME -->	 
  	WHERE M.USERNAME=#{username}
  </select>
  
  <!-- 최종 타입은 MemberVO를 리턴할 것이다. -->
  <resultMap type="MemberVO" id="getMemberResult">
  	<!-- setUserName에다 집어줘 -->
  	<id column="USERNAME" property="username"/>
  	<result column="PASSWORD" property="password"/>
  	<result column="NAME" property="name"/>
  	<result column="EMAIL" property="email"/>
  	<result column="BIRTH" property="birth"/>
  	<result column="JOINDATE" property="joinDate"/>
  	<!-- 1대 1 관계인 어소세이션이 먼저 와야 하고, 그 다음에 collection이 와야한다. 순서 중요함!!!  -->
  	<!-- property roleName 정보가 필요하다. -->
  	<collection property="roleVOs" javaType="List" ofType="RoleVO">
  		<id column="ROLENUM" property="roleNum"/>
  		<result column="ROLENAME" property="roleName"/>
  	</collection>

  </resultMap>
  
  
  </mapper>